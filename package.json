{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-pdf": "node scripts/generate-cv-pdf.mjs",
    "build:full": "astro build && node scripts/generate-cv-pdf.mjs"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "astro-i18n": "^2.2.4"
  },
  "devDependencies": {
    "puppeteer": "^24.38.0"
  }
}
